Not done yet:

Done:

Gameplay
-Generate a maze with multiple possible paths
-Add player health and deal damage when an enemy touches the player
-Add a simple attack system
-Add a cursor to show where the player is aiming
-Create a Player class with a grid position (i, j)
-Add movement between cells using the keyboard
-Prevent movement through walls using wall_north, wall_south, wall_east, wall_west
-Add a visible exit tile and detect when the player reaches it
-Create an Enemy class with automatic pathfinding
-Create a map item that reveals the exit
-Create collectible items in the maze (coins)
-Implement a progressive level system
-Create a story
-Implement the story with text

Core System
-Replace the old matplotlib display with a Pygame window
-Keep the Cell and Maze classes to preserve the original project structure
-Preserve the stack-based maze generation algorithm (Stack)
-Add a simple main() function to launch the window and render the maze
-Allow maze regeneration with a key like R for quick testing

UI / Menu
-Display a win screen when the player reaches the exit
-Create a start menu with at least Start and Quit
-Display a lose screen when health reaches 0
-Show basic information on screen: health, controls, game state

Polish
-Rename variables and methods into proper English where needed
-Add images for the maze, player, enemies, and exit
-Test each feature individually instead of implementing everything at once
